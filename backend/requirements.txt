# SKYKART Backend Dependencies
# Fixed for Render — all packages have pre-built wheels, zero Rust compilation

# Web Framework
fastapi==0.115.6
uvicorn[standard]==0.32.1

# Database
sqlalchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.12

# Pydantic v2 — latest stable, has pre-built wheels for ALL Python versions
pydantic==2.10.3
pydantic-settings==2.6.1
email-validator==2.2.0

# HTTP Client
httpx==0.28.1

# Environment
python-dotenv==1.0.1

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
